package app.majodesk.domain.model

enum class DistanceUnit { KILOMETER, METER }  // видимо только для бега
enum class WeightUnit { KILOGRAM, TON }       // только для гантелей, спортзала
enum class TimeUnit { HOUR, MINUTE, SECOND }